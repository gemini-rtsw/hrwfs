# Generated by applSetup.pl / the UAE build. These repos predate git and have
# no .cvsignore either; the top-level Makefile's `release` target lists the
# same set, which is where this came from.
/config/
/bin/
/lib/
/include/
/dbd/
/data/
/javalib/
O.*/
.applTop
Distfile
UAE.dist
resource.def
cad.rc
local.vws.bak
/local.vws
*.log
hrwfsBuild.log

# editor droppings
*%
*~
.*.sw[a-p]

# build_rpm.sh output
rpms/

# SVN dumps and staged production trees are escrow -- keep them outside the
# repo (~/work/hrwfs-production, ~/work/hrwfs-migration), never committed.
*.dump
*.dump.gz

# build-dep-rpms.sh stages patches/ here at build time
tools/linux-build/rpm/staged-patches/
tools/linux-build/rpm/out/
